spring:
  application:
    name: gateway-service

server:
  port: 8000

eureka-server:
  host: localhost
  port: 8761
  postfix: default

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://admin:1234@${eureka-server.host}:${eureka-server.port}/eureka

  instance:
    prefer-ip-address: true

management:
  endpoint:
    gateway:
      enabled: true

  endpoints:
    web:
      exposure:
        include: gateway

55g:
  log:
    path: .
logging:
  file:
    name: ${55g.log.path}/gateway.log
---
spring:
  config:
    activate:
      on-profile: live

eureka-server:
  postfix: live

logging:
  file:
    name: ${55g.log.path}/gateway-live.log
  logback:
    rollingpolicy:
      max-history: 100
      file-name-pattern: ${55g.log.path}/gateway-live-%d{yyyy-MM-dd_HH:mm}.%i.log
      max-file-size: 100KB
  level:
    root: info
    com.S5G.gateway: info

55g:
  log:
    path: /logs/live/gateway
---
spring:
  config:
    activate:
      on-profile: dev

eureka-server:
  port: 8762
  postfix: dev

server:
  port: 8050

logging:
  file:
    name: ${55g.log.path}/gateway-dev.log
  logback:
    rollingpolicy:
      max-history: 30
      file-name-pattern: ${55g.log.path}/gateway-dev-%d{yyyy-MM-dd_HH:mm}.%i.log
      max-file-size: 100KB
  level:
    root: debug
    com.S5G.gateway: trace

55g:
  log:
    path: /logs/dev/gateway
---
spring:
  config:
    activate:
      on-profile: docker

eureka-server:
  host: 55g-eureka

eureka:
  client:
    service-url:
      defaultZone: http://admin:1234@${eureka-server.host}-${eureka-server.postfix}:${eureka-server.port}/eureka